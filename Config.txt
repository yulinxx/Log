# 公共依赖项配置文件
# 仅用于集中管理所有子项目的依赖项路径配置

# VCPKG路径配置
if(WIN32)
    set(VCPKG_DIR "D:/Install/vcpkg/" CACHE PATH "VCPKG安装目录")
elseif(UNIX AND NOT APPLE)
    set(VCPKG_DIR "/home/x/Install/vcpkg/" CACHE PATH "VCPKG安装目录")
elseif(APPLE)
    set(VCPKG_DIR "/path/to/mac/vcpkg/" CACHE PATH "VCPKG安装目录")
endif()
